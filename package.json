{
  "name": "yneva-wiki",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "python -m tools.pipeline.build && astro build && npx pagefind --site dist",
    "tokens:check": "node tools/check-token-discipline.mjs",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.17.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "marked": "^12.0.2",
    "pagefind": "^1.1.1"
  }
}
